# Changelog

All notable changes to this project will be documented in this file.

## [0.1.0] - Unreleased

### Added
- Repo scaffold (`pyproject.toml`, `LICENSE`, `README.md`).
- `ColumnSpec` frozen dataclass + `to_bq_schema` / `spec_names` helpers in `ffl_bigquery/schema.py`.
- `INGESTED_AT_SPEC` shared lineage column spec.
